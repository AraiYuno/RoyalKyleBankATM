@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-md-6 margin-top-20">
        <a href="#" class="btn btn-primary btn-lg btn-block active"><span class="glyphicon glyphicon-circle-arrow-down"></span>
          Withdrawal</a>
        <a href="#" class="btn btn-primary btn-lg btn-block active">
            <span class="glyphicon glyphicon-usd"></span>
            Quick Cash $200
        </a>
        <a href="@Url.Action("Details","CheckingAccount")" class="btn btn-primary btn-lg btn-block active">
            <span class="glyphicon glyphicon-question-sign"></span>
            Balance Details
        </a>
    </div>

    <div class="col-md-6 margin-top-20">
        <a href="#" class="btn btn-primary btn-lg btn-block active">
            <span class="glyphicon glyphicon-print"></span>
            Bank Statement
        </a>
        <a href="#" class="btn btn-primary btn-lg btn-block active">
            <span class="glyphicon glyphicon-transfer"></span>
            E-Transfer
        </a>
        <a href="@Url.Action("Deposit", "Transaction", new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-lg btn-block active">
            <span class="glyphicon glyphicon-circle-arrow-up"></span>
            Deposit
        </a>
    </div>


</div>